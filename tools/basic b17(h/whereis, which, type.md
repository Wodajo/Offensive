`whereis bash` - search $PATH, $MANPATH and standard Linux places
locate binary, source & manual


`which bash` - search the PATH
locate binary

`type bash` - type of command (locate binary, shell builtin, alias)