`whoami /all` - username, groups, privileges info
if domain user - fqdn & upn (user principal name)
`ver` - OS version

`tasklist /v`

`ipconfig /all`
`netstat -an` - `-a` all connections & listening ports,
`-n` addresses & ports in numerical form